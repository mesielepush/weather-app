const dataHandler = (data)=>{
    const selector   = document.getElementById; 
    const weather    = document.getElementById;
    const temp       = document.getElementById;
    const temp_max   = document.getElementById;
    const temp_min   = document.getElementById;
    const temp_feels = document.getElementById;
    const country    = document.getElementById; 
}